on: repository_dispatch
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git subtree add --prefix=sub/libpng https://github.com/chaowenGUO/aiohttp master --squash
          git push
