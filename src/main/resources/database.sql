create table EMPLOYEE_SALARY (DEPTNO   CHAR(3)      NOT NULL,
                              DEPTNAME VARCHAR(36)  NOT NULL,
                              EMPNO    CHAR(6)      NOT NULL,
                              ALARY   DECIMAL(9,2) NOT NULL WITH DEFAULT)
