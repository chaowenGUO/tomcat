plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    compile 'io.vertx:vertx-web:4.0.0.Beta3'
    compile 'io.vertx:vertx-pg-client:4.0.0.Beta3'
}

task copyDependencies(type: Copy) {
    from configurations.default into 'copyDependencies'
}
