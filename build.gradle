plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    compile 'io.vertx:vertx-web:latest.release'
    compile 'io.vertx:vertx-pg-client:latest.release'
}

task copyDependencies(type: Copy) {
    from configurations.default into 'copyDependencies'
}
